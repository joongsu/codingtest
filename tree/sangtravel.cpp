#include <iostream> // 백준-9372 상근이의 여행
                    // 신장트리의 정의에 관한 문제
using namespace std;

int main(){


    int T;
    cin >> T;

    for(int i = 0 ; i < T ; i++){
        int n,m;
        cin >> n >> m;
        for(int j=0; j < m ; j++){
            int a,b;
            cin >> a >> b;
        }
        cout << n-1 << '\n';
    }
}